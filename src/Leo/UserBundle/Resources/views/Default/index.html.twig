{% extends 'default/index.html.twig' %}
{% block body %}
    {{parent()}}
    <a href="{{path("user_index")}}">{{ 'Users'|trans({}, 'LeoUserBundle') }}</a>
    <a href="{{path("role_index")}}">{{ 'Roles'|trans({}, 'LeoUserBundle') }}</a>{#
    <a href="{{path("leo_user_logout")}}">Logout</a>#}
{% endblock %}