{% extends 'default/index.html.twig' %}
{% block body %}
    {{parent()}}
    <a href="{{path("author_index")}}">{{ 'Author'|trans({}, 'LeoBlogBundle') }}</a>
    <a href="{{path("post_index")}}">{{ 'Post'|trans({}, 'LeoBlogBundle') }}</a>
    <a href="{{path("comment_index")}}">{{ 'Comment'|trans({}, 'LeoBlogBundle') }}</a>
{% endblock %}